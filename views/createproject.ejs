<%- include('partials/head', { title: 'Create Project', logs: locals.log, pfp: locals.pfp, name: locals.name }) %>

<a href="/dashboard" class="back-btn">← Back to Dashboard</a>

<div class="settings-header">
  <h1>./Create Project</h1>
  <span>Publish a new project to your MakerCreate portfolio</span>
</div>

<div class="settings-section">
  <span class="settings-section-title">Project Details</span>
  <span class="settings-section-subtitle">Fill in the information below to get started.</span>
  
  <form method="POST" action="/createprj" class="form" id="createForm" enctype="multipart/form-data">
      <label for="image">Project Image</label>
      <input type="file" name="image" id="image" accept="image/*">
      
      <input type="text" name="slackId" id="slackId" value="<%= locals.slackId %>" hidden required>
      
      <label for="name">Project Name*</label>
      <input type="text" name="name" id="name" placeholder="E.g., 80s Photobooth" required>
      
      <label for="description">Project Description*</label>
      <textarea name="description" id="description" placeholder="A brief description of your project..." required></textarea>
      
      <label>Project Category*</label>
      <div style="display: flex; gap: 0.5rem; margin-top: 0.2rem;">
        <input type="radio" name="category" id="CAD" value="CAD" required hidden>
        <label class="radio-btn" for="CAD">CAD</label>

        <input type="radio" name="category" id="Hardware" value="Hardware" required hidden>
        <label class="radio-btn" for="Hardware">Hardware</label>
      </div>

      <button class="aabtn" type="submit" style="margin-top: 1rem;">Create Project</button>
  </form>
</div>

<script>
    document.getElementById('createForm').addEventListener('submit', function() {
        const btn = this.querySelector('button[type="submit"]');
        btn.disabled = true;
        btn.innerText = 'Creating...';
    });
</script>
<%- include('partials/foot') %>